# Define three vectors
a <- c(1, 2, 3)
b <- c(4, 5, 6)
c <- c(7, 8, 9)

# Combine them into a 3×3 matrix (columns represent vectors)
mat <- cbind(a, b, c)

# Print the matrix
cat("3×3 Matrix:\n")
print(mat)
